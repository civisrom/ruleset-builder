# 🚀 Ruleset Builder v3.0 — Универсальный генератор правил

**Мощный инструмент для создания ruleset для Sing-Box и Mihomo с графическим и командным интерфейсом.**

---

## ✨ Основные возможности

### 🎯 Поддержка форматов
- **Sing-Box**: `.json` (Rule Set) и `.srs` (скомпилированный бинарный формат)
- **Mihomo**: `.mrs` (Mihomo Rule Set) — бинарный формат

### 🛠️ Функциональность
- ✅ Графический интерфейс (GUI) с интуитивным управлением
- ✅ Командная строка (CLI) для автоматизации
- ✅ Работа с большими файлами (оптимизированное чтение)
- ✅ Валидация входных данных (домены, IP, регулярные выражения)
- ✅ Готовые шаблоны (блокировка рекламы, социальные сети, и т.д.)
- ✅ Сохранение и загрузка пользовательских шаблонов
- ✅ Превью результата перед генерацией
- ✅ Статистика и подсчёт записей
- ✅ Многопоточная загрузка файлов
- ✅ Счётчики строк в реальном времени

---

## 📦 Установка

### Требования
- **Python 3.7+**
- **tkinter** (обычно включен в Python)
- **sing-box.exe** (опционально, для компиляции `.srs`)

### Установка зависимостей

```bash
pip install -r requirements.txt
```

*Примечание: Проект использует встроенные библиотеки Python, дополнительные зависимости не требуются.*

---

## 🎮 Использование

### Режим GUI (графический интерфейс)

Запустите программу без аргументов:

```bash
python ruleset_builder.py
```

#### Основные шаги:

1. **Выберите sing-box.exe** (если планируете компилировать `.srs`)
2. **Заполните правила** во вкладках:
   - 🌐 **Domains** — домены, суффиксы, ключевые слова, regex
   - 🔢 **IP Addresses** — IP CIDR, source IP
   - ⚙️ **Processes** — пути к процессам, пакеты Android
   - 📡 **Network** — сетевые параметры
3. **Выберите формат вывода**: JSON, SRS, MRS
4. **Нажмите "Генерировать Ruleset"**

#### Дополнительные функции:

- **📋 Шаблоны**: Применяйте готовые шаблоны (блокировка рекламы, соц. сети)
- **👁️ Превью**: Просмотр JSON структуры перед генерацией
- **📊 Статистика**: Подробная статистика по всем категориям
- **✓ Валидация**: Проверка корректности доменов, IP, regex

---

### Режим CLI (командная строка)

Для автоматизации и интеграции в скрипты:

```bash
python ruleset_builder.py -o output_name -f json --domain domains.txt --ip-cidr ips.txt
```

#### Аргументы командной строки:

| Аргумент | Описание |
|----------|----------|
| `-o`, `--output` | Имя выходного файла (без расширения) |
| `-f`, `--format` | Формат: `json`, `srs`, `mrs` (по умолчанию: `json`) |
| `--singbox` | Путь к sing-box.exe (для компиляции `.srs`) |
| `--domain` | Файл с точными доменами |
| `--domain-suffix` | Файл с суффиксами доменов (`.ru`, `.com`) |
| `--domain-keyword` | Файл с ключевыми словами |
| `--domain-regex` | Файл с regex паттернами |
| `--ip-cidr` | Файл с IP CIDR |
| `--source-ip-cidr` | Файл с source IP CIDR |
| `--validate` | Включить валидацию данных |

#### Примеры использования CLI:

**Создание JSON ruleset:**
```bash
python ruleset_builder.py -o my_rules -f json \
  --domain domains.txt \
  --domain-suffix suffixes.txt \
  --ip-cidr ips.txt
```

**Создание и компиляция .srs:**
```bash
python ruleset_builder.py -o my_rules -f srs \
  --singbox "C:\sing-box\sing-box.exe" \
  --domain domains.txt \
  --validate
```

**Создание .mrs для Mihomo:**
```bash
python ruleset_builder.py -o mihomo_rules -f mrs \
  --domain domains.txt \
  --ip-cidr ips.txt
```

---

## 📝 Формат входных файлов

Все входные файлы должны быть текстовыми, одна запись на строку:

### Пример `domains.txt`:
```
google.com
youtube.com
facebook.com
```

### Пример `suffixes.txt`:
```
.ru
.рф
.com
.org
```

### Пример `ips.txt`:
```
10.0.0.0/8
192.168.0.0/16
8.8.8.8/32
```

### Пример `regex.txt`:
```
^stun\..+
.*\.torrent$
^.*tracker.*$
```

**Комментарии**: Строки, начинающиеся с `#`, игнорируются.

---

## 🎨 Готовые шаблоны

Программа включает несколько предустановленных шаблонов:

1. **Блокировка рекламы** — популярные рекламные домены
2. **Российские домены** — `.ru`, `.рф`, `.su`
3. **Социальные сети** — Facebook, Twitter, Instagram
4. **Стриминг сервисы** — YouTube, Netflix, Twitch
5. **Локальные сети** — частные IP диапазоны

Вы можете создавать и сохранять свои собственные шаблоны!

---

## 📊 Структура выходного JSON

```json
{
  "version": 1,
  "rules": [
    {
      "domain": ["example.com"],
      "domain_suffix": [".ru", ".com"],
      "domain_keyword": ["ads", "tracker"]
    },
    {
      "ip_cidr": ["10.0.0.0/8", "192.168.0.0/16"]
    }
  ]
}
```

---

## 🔧 Формат .mrs (Mihomo)

`.mrs` файл — это бинарный формат для Mihomo с оптимизированной структурой:

```
Заголовок: "MRS" + версия (1 байт)
Количество правил: 4 байта (unsigned int, big-endian)

Для каждого правила:
  Тип правила: 1 байт
    1 = DOMAIN
    2 = DOMAIN-SUFFIX
    3 = DOMAIN-KEYWORD
    4 = IP-CIDR
  Длина значения: 2 байта (unsigned short, big-endian)
  Значение: N байт (UTF-8)
```

---

## ⚙️ GitHub Actions

Для автоматической сборки создайте `.github/workflows/build.yml`:

```yaml
name: Build Ruleset

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Generate ruleset
      run: |
        python ruleset_builder.py -o output/ruleset -f json \
          --domain data/domains.txt \
          --ip-cidr data/ips.txt \
          --validate
    
    - name: Upload artifacts
      uses: actions/upload-artifact@v3
      with:
        name: rulesets
        path: output/
```

---

## 🐛 Обработка ошибок

Программа включает комплексную обработку ошибок:

- ✅ Валидация доменов (RFC-совместимые)
- ✅ Валидация IP CIDR (IPv4)
- ✅ Проверка регулярных выражений
- ✅ Обработка больших файлов (построчное чтение)
- ✅ Таймауты для компиляции
- ✅ Детальные сообщения об ошибках в лог

---

## 💡 Полезные советы

### Оптимизация производительности

1. **Большие файлы**: Используйте CLI режим для файлов > 100,000 строк
2. **Валидация**: Отключайте валидацию для проверенных данных
3. **Формат**: `.srs` и `.mrs` быстрее загружаются, чем JSON

### Лучшие практики

- Используйте `domain_suffix` вместо множества точных доменов
- Группируйте IP в большие CIDR блоки
- Регулярные выражения работают медленнее — используйте с умом
- Комментируйте входные файлы для лучшей читаемости

---

## 📄 Лицензия

MIT License — свободное использование для любых целей.

---

## 🤝 Вклад в проект

Pull requests приветствуются! Для крупных изменений сначала откройте issue.

### Приоритетные улучшения:
- [ ] Поддержка IPv6
- [ ] Импорт из Clash/V2Ray форматов
- [ ] Drag & Drop файлов в GUI
- [ ] Тёмная тема
- [ ] Локализация (EN/RU/CN)

---

## 📞 Поддержка

Если вы нашли баг или у вас есть предложение — создайте issue на GitHub.

---

**Сделано с ❤️ для сообщества Sing-Box и Mihomo**
