# 🚀 Быстрый старт — Ruleset Builder v3.0

## 📦 Что нового в версии 3.0?

### Основные улучшения:
✅ **Поддержка Mihomo .mrs** — генерация бинарных ruleset для Mihomo
✅ **Оптимизация больших файлов** — построчное чтение с прогресс-индикацией
✅ **Валидация данных** — автоматическая проверка доменов, IP и regex
✅ **Готовые шаблоны** — быстрое заполнение типовых правил
✅ **Превью результата** — просмотр JSON перед генерацией
✅ **Статистика** — детальная информация о количестве правил
✅ **Дополнительные утилиты** — конвертация, объединение, анализ

---

## ⚡ Быстрая установка

### 1. Скачайте проект
```bash
git clone https://github.com/yourusername/ruleset-builder.git
cd ruleset-builder
```

### 2. Установите зависимости (опционально)
```bash
pip install -r requirements.txt
```
*Проект работает на чистом Python без внешних зависимостей!*

### 3. Запустите программу
```bash
python ruleset_builder.py
```

---

## 📖 Примеры использования

### Пример 1: Создание простого ruleset через GUI

1. Запустите `python ruleset_builder.py`
2. Перейдите на вкладку **"Domains"**
3. В поле **"Точные домены"** введите:
```
google.com
youtube.com
facebook.com
```
4. В поле **"Суффиксы"** введите:
```
.ads.com
.tracking.net
```
5. Нажмите **"🚀 Генерировать Ruleset"**
6. Готово! Файл `ruleset.json` создан

---

### Пример 2: Использование готового шаблона

1. Откройте программу
2. Перейдите на вкладку **"📋 Шаблоны"**
3. Нажмите **"📋 Блокировка рекламы"**
4. Данные автоматически заполнятся
5. Нажмите **"🚀 Генерировать Ruleset"**

---

### Пример 3: Импорт из файла (большой список доменов)

1. Создайте файл `my_domains.txt`:
```txt
google.com
facebook.com
twitter.com
# ... 10,000 строк ...
```

2. В программе перейдите на вкладку **"Domains"**
3. Нажмите кнопку **"📁 Файл"** рядом с полем
4. Выберите `my_domains.txt`
5. Данные загрузятся автоматически

---

### Пример 4: Генерация .srs (скомпилированный формат)

1. Скачайте `sing-box.exe` с официального сайта
2. В программе нажмите **"📁"** рядом с полем "Sing-box"
3. Выберите `sing-box.exe`
4. Включите опцию **"✓ Компилировать .srs"**
5. Заполните правила
6. Нажмите **"🚀 Генерировать Ruleset"**
7. Будут созданы: `ruleset.json` и `ruleset.srs`

---

### Пример 5: Генерация .mrs для Mihomo

1. Заполните правила (только домены и IP поддерживаются)
2. Включите опцию **"✓ Генерировать .mrs (Mihomo)"**
3. Нажмите **"🚀 Генерировать Ruleset"**
4. Файл `ruleset.mrs` будет создан

---

### Пример 6: Использование CLI для автоматизации

```bash
# Создание JSON из файлов
python ruleset_builder.py \
  -o my_rules \
  -f json \
  --domain data/domains.txt \
  --domain-suffix data/suffixes.txt \
  --ip-cidr data/ips.txt \
  --validate

# Создание и компиляция .srs
python ruleset_builder.py \
  -o my_rules \
  -f srs \
  --singbox /path/to/sing-box.exe \
  --domain data/domains.txt

# Создание .mrs для Mihomo
python ruleset_builder.py \
  -o mihomo_rules \
  -f mrs \
  --domain data/domains.txt \
  --ip-cidr data/ips.txt
```

---

## 🛠️ Дополнительные утилиты

### Конвертация из Clash
```bash
python ruleset_utils.py convert \
  --from clash \
  --input clash_config.yaml \
  --output singbox_ruleset.json
```

### Объединение нескольких ruleset
```bash
python ruleset_utils.py merge \
  --inputs rules1.json rules2.json rules3.json \
  --output merged_rules.json
```

### Анализ ruleset
```bash
python ruleset_utils.py analyze \
  --input my_rules.json \
  --find-duplicates
```

### Сравнение двух ruleset
```bash
python ruleset_utils.py compare \
  --file1 old_rules.json \
  --file2 new_rules.json
```

### Оптимизация ruleset
```bash
python ruleset_utils.py optimize \
  --input large_ruleset.json \
  --output optimized_ruleset.json
```

---

## 📊 Структура проекта

```
ruleset-builder/
├── ruleset_builder.py      # Главная программа (GUI + CLI)
├── ruleset_utils.py         # Дополнительные утилиты
├── requirements.txt         # Зависимости (пустой для этого проекта)
├── README.md                # Основная документация
├── TEMPLATES_GUIDE.md       # Руководство по шаблонам
├── QUICKSTART.md           # Этот файл
├── .gitignore              # Git исключения
├── .github/
│   └── workflows/
│       └── build.yml       # GitHub Actions для CI/CD
└── data/                   # Примеры данных
    ├── domains.txt
    ├── suffixes.txt
    └── ips.txt
```

---

## 🎯 Типовые сценарии

### Сценарий 1: Корпоративная сеть
**Цель**: Блокировка рекламы и социальных сетей

1. Используйте шаблон **"Блокировка рекламы"**
2. Добавьте шаблон **"Социальные сети"**
3. Добавьте внутренние домены компании в "Точные домены"
4. Генерируйте ruleset

### Сценарий 2: Домашний роутер
**Цель**: Фильтрация контента для детей

1. Импортируйте список "adult content" доменов
2. Добавьте игровые платформы по необходимости
3. Настройте сетевые параметры (Wi-Fi)
4. Генерируйте и загрузите на роутер

### Сценарий 3: VPN клиент
**Цель**: Маршрутизация трафика через VPN

1. Укажите домены, требующие VPN (соц. сети)
2. Добавьте IP диапазоны
3. Настройте правила для мобильных приложений
4. Генерируйте .srs для оптимальной производительности

---

## ⚠️ Частые проблемы и решения

### Проблема 1: "sing-box не найден"
**Решение**: Укажите полный путь к `sing-box.exe` в поле "Sing-box"

### Проблема 2: "Ошибка валидации домена"
**Решение**: Проверьте формат:
- ✅ Правильно: `example.com`
- ❌ Неправильно: `http://example.com`, `example .com`

### Проблема 3: "Файл слишком большой"
**Решение**: Используйте CLI режим для файлов > 100,000 строк

### Проблема 4: ".mrs не работает в Mihomo"
**Решение**: Убедитесь, что используете последнюю версию Mihomo с поддержкой .mrs

---

## 💡 Советы по оптимизации

### 1. Используйте суффиксы вместо точных доменов
❌ Плохо:
```
mail.google.com
drive.google.com
docs.google.com
```

✅ Хорошо:
```
.google.com
```

### 2. Группируйте IP в CIDR блоки
❌ Плохо:
```
192.168.1.1/32
192.168.1.2/32
192.168.1.3/32
```

✅ Хорошо:
```
192.168.1.0/24
```

### 3. Избегайте избыточных regex
Regex медленнее обычных правил. Используйте только когда необходимо.

---

## 📈 Лучшие практики

1. **Комментируйте данные**: Используйте `#` для пояснений в файлах
2. **Версионируйте правила**: Используйте Git для отслеживания изменений
3. **Тестируйте перед деплоем**: Используйте превью для проверки
4. **Регулярно обновляйте**: Рекламные домены постоянно меняются
5. **Используйте готовые списки**: AdGuard, EasyList и др.

---

## 🔗 Полезные ссылки

- [Sing-Box документация](https://sing-box.sagernet.org/)
- [Mihomo GitHub](https://github.com/MetaCubeX/mihomo)
- [AdGuard DNS фильтры](https://adguard.com/)
- [EasyList](https://easylist.to/)

---

## 📞 Получить помощь

- **GitHub Issues**: Сообщите о баге или запросите функцию
- **Discussions**: Задайте вопрос сообществу
- **Wiki**: Дополнительные примеры и гайды

---

## 🎉 Готово!

Теперь вы знаете всё необходимое для работы с Ruleset Builder.

**Следующие шаги:**
1. Попробуйте создать свой первый ruleset
2. Изучите готовые шаблоны
3. Настройте автоматизацию через GitHub Actions
4. Поделитесь своими шаблонами с сообществом

---

**Удачи! Если нужна помощь — мы всегда рады помочь! 🚀**
